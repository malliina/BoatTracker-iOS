name: Release
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-11
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Prepare credentials
        run: |
          echo "$APPSTORE_CONNECT_API_KEY" > AuthKey_VG8XUGYMD6.p8.b64
          base64 -d -i AuthKey_VG8XUGYMD6.p8.b64 > AuthKey_VG8XUGYMD6.p8
          echo "$CERTIFICATE_P12" > certificate.p12.b64
          base64 -d -i certificate.p12.b64 > certificate.p12
          echo "$CREDENTIALS_PLIST" > Credentials.plist.b64
          base64 -d -i Credentials.plist.b64 > BoatTracker/Credentials.plist
          echo "$NETRC" > netrc.b64
          base64 -d -i netrc.b64 > ~/.netrc
        env:
          APPSTORE_CONNECT_API_KEY: ${{ secrets.APPSTORE_CONNECT_API_KEY }}
          CERTIFICATE_P12: ${{ secrets.CERTIFICATE_P12 }}
          CREDENTIALS_PLIST: ${{ secrets.CREDENTIALS_PLIST }}
          NETRC: ${{ secrets.NETRC }}
      - name: Release
        run: fastlane release
        env:
          CERTIFICATE_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
          FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${{ secrets.FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD }}
