import Foundation

enum MapCommand {
  case toggleFollow, clearAll
}

enum MapMode {
  case fit
  case follow
  case stay
}

enum HorizontalPlacement {
  case left
  case center
  case right
}

enum VerticalPlacement {
  case top
  case bottom
}
